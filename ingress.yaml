
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: argocd-server
  namespace: argocd
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 80
